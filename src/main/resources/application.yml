server:
  port: 8081

spring:
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
  security:
    user:
      name: user
      password: user

  datasource:
    url: jdbc:mysql://localhost:3306/security
    username: root
    password: root@123
  jpa:
    hibernate:
      ddl-auto: update